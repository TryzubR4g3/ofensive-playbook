# Stored-XSS
>As the name infers, the payload is stored on the web application (in a database, for example) and then gets run when other users visit the site or web page.

## When to Use

- The payload is stored on the web application.
- The payload gets run when other users visit the site or web page.